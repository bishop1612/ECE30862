import java.util.Scanner;
import java.util.Random;

public class multiply {
	private static int N;

	public multiply(int n) {
		if(n%2!=0) {
			
		}
		N = n;
		// TODO Auto-generated constructor stub
	}
	
	public int[][] formmatrix() {
		Random randomGenerator = new Random();
		int [][] formmatrix = new int[N][N];
		int i,j = 0;
		for(i = 0;i < N;i++){
			for(j = 0;j < N;j++){
				formmatrix[i][j] = randomGenerator.nextInt(100);
			}
		}
		return formmatrix;
		
		// TODO Auto-generated constructor stub
	}
	

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		boolean changed = false;
		Scanner reader = new Scanner(System.in);  // Reading from System.in
		System.out.println("Enter a number: ");
		int n = reader.nextInt();// Scans the next token of the input as an int.
		while(true) {
            try {
                changed = false;
    
            } catch (Exception e) {
                System.out.print(MulThread.N + " is an unacceptable value of N, please enter a new value: ");

                MulThread.N = s.nextInt();
                changed = true;
            }

            if(!changed) {
                break;
            }
        }
		multiply mp = new multiply(n);
		int [][] B = mp.formmatrix();
		int [][] C = mp.formmatrix();
		int i,j = 0;
		for(i = 0;i < N;i++){
			for(j = 0;j < N;j++){
				System.out.println(B[i][j] + " " + C[i][j]);
			}
		}
		reader.close();

	}

}
